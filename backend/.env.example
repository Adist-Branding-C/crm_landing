# Server
PORT=5000

# SMTP credentials — fill in your ACTUAL mailbox credentials
SMTP_USER=support@leadistcrm.com
SMTP_PASS=ngpirgpqdiirewfj
SMTP_HOST=smtp.hostinger.com
SMTP_PORT=587
SMTP_SECURE=false

# Comma-separated list of allowed frontend origins
CORS_ORIGIN=http://localhost:3000,http://localhost:3001

# Destination mailbox for demo requests
DEMO_REQUEST_TO=tech@adist.in
